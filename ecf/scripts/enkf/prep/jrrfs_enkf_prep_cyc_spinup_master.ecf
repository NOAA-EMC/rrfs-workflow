#!/bin/bash
#PBS -N rrfs_enkf_prep_cyc_spinup_mem@enkf_prep_cyc_spinup_member@_%CYC%
#PBS -j oe
#PBS -S /bin/bash
#PBS -q %QUEUE%
#PBS -A %PROJ%-%PROJENVIR%
#PBS -l walltime=00:20:00
#PBS -l place=vscatter:shared,select=1:ncpus=5:mem=500G
#PBS -l debug=true
#SBATCH --job-name=rrfs_enkf_prep_cyc_spinup_mem@enkf_prep_cyc_spinup_member@_%CYC%
#SBATCH --account=%PROJ%
#SBATCH --qos=%QUEUE%
#SBATCH --partition=%PARTITION:u1-compute%
#SBATCH --time=00:20:00
#SBATCH --nodes=1
#SBATCH --ntasks-per-node=5
# WCOSS2 asks for mem=500G, more than an Ursa u1-compute node has (376G), so take the whole node's memory
#SBATCH --mem=0
#SBATCH --output=%ECF_JOBOUT%

model=rrfs
export MACHINE=%MACHINE:WCOSS2%
export cyc="%CYC%"
%include <head.h>
%include <envir-p1.h>
############################################################
# Load modules
############################################################
# Cray modules for WCOSS2; on Ursa, head.h loads modulefiles/run_ursa.lua instead
if [ "${MACHINE}" = "WCOSS2" ]; then
module load intel/${intel_ver}
module load craype/${craype_ver}
module load ve/rrfs/${rrfs_pythonve_ver}
module load cray-mpich/${cray_mpich_ver}
module load cray-pals/${cray_pals_ver}
module load hdf5-D/${hdf5_ver}
module load netcdf-D/${netcdf_ver}
module load pnetcdf-D/${pnetcdf_ver}
module load bacio/${bacio_ver}
module load w3emc/${w3emc_ver}
module load sp/${sp_ver}
module load ip/${ip_ver}
module load sigio/${sigio_ver}
module load sfcio/${sfcio_ver}
module load nemsio/${nemsio_ver}
module load libjpeg/${libjpeg_ver}
module load libaec/${libaec_ver}
module load g2c/${g2c_ver}
module load libjpeg-turbo/${libjpeg_turbo_ver}
module load wgrib2/${wgrib2_ver}
module load udunits/${udunits_ver}
module load gsl/${gsl_ver}
module load nco/${nco_ver}
module load cfp/${cfp_ver}
fi

export WGF="%WGF%"
export MEMBER_NAME="%MEMBER_NAME%"
export CYCLE_TYPE="%CYCLE_TYPE%"
export CYCLE_SUBTYPE="%CYCLE_SUBTYPE%"
module list
############################################################
# CALL executable job script here
############################################################
${HOMErrfs}/jobs/JRRFS_PREP_CYC

%include <tail.h>

%manual
%end
